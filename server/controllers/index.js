const authController = require('./authController');
const usersController = require('./usersController');
const resourceListController = require('./resourceListController');

module.exports = {
    authController,
    usersController,
    resourceListController
};